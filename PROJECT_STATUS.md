# PROJECT_STATUS.md
最終更新: 2026-08-07

---

## プロジェクト目的

試作課の工数・労務費を日報から自動集計し、上長・生産管理課が参照できる形にする。

- **短期目的（現フェーズ）**: まず記録・蓄積。「何に何時間使ったか」を定量化する
- **中期目的**: ブランド・企画別のリソース配分を可視化し、上長の事業判断をサポート
- **長期目的**: フェーズ別工数トレンドの分析（なぜ時間がかかったか）、改善施策立案

> 上長の言葉: 「感覚で試作課を運営する段階を卒業して、数字で試作課を経営したい」

---

## システム構成（2026-08-06 時点）

### 全体構成（5つの要素＋検索サイドバー）

| # | ファイル | 役割 | 中身 |
|---|---------|------|------|
| 1 | 採番表（実装済み・稼働中） | 案件登録の原本。試作番号（製番）を発行する起点 | A〜F：試作番号（`TK-G26-0001`形式）／シーズン・型振・VMD／ブランド／区分（製品/販促）／企画名／製品名。G：登録日、H：備考（上長の自由記述専用）、I：類似候補（システムが同企画内の既存番号をヒント表示。2026-08-03追加）。担当者・フェーズ・日程・ステータスは持たない（進行管理は外部スケジュール表側に一本化） |
| 2 | 外部スケジュール表（嶋谷さん管理・別ファイル） | 進行管理の原本（フェーズ・実施予定日・ステータス） | REPORT_CONFIG.scheduleSSId。上長がA列（試作番号）を入力/選択すると、採番表を参照してB〜F列が自動補完される（プル型。2026-08-03、行の自動追加は廃止）。「🔍 採番表検索」サイドバー（`SeibanSearch.gs`+`SearchSidebar.html`）を同SSに追加済み（2026-08-06）：試作番号・ブランド・企画名・製品名で採番表を検索可能 |
| 3 | 試作日報：ログ取り | 日報アプリ本体・マスタ・生データ | 日報ログ（B列に製番を記録。2026-08-05） / スケジュール（内部製品マスタ、外部スケジュール表から自動同期）/ ステージ / フェーズ中分類 / その他種別 / 職人 / 休日 |
| 4 | WeeklyReport | 集計データ（推移・分析用） | ①週次推移 ②職人別週次 ③月別推移 ④職人別月次 ⑤ブランド別 ⑥企画別 ⑦製品別 ⑧製品×職人別 |
| 5 | 試作課：日報サマリー | 共有・閲覧専用ダッシュボード | 先頭2タブ＝最新月の⓪日報サマリー/⓪販促サマリー（上書き型）＋月別アーカイブタブ（グレー・自動保存） |

### データの流れ（2026-08-03改訂：プッシュ型→プル型。実装・動作確認・GAS反映済み）

採番表に登録すると試作番号が自動発行される（①）。上長がスケジュールを組みたいタイミングで外部スケジュール表に自分で行を作り、A列にその試作番号を入力/選択すると、採番表を参照してB〜F列が自動補完される（②プル型・システムが行を作ることはしない）。以降は外部スケジュール表の製番が内部製品マスタへ10分おき自動同期（③、既存のまま）→ 日報入力（製番を裏側でルックアップしログに記録）→ 日報ログ → 集計レポート（4・5）と続く。過去に登録済みの試作番号は「🔍 採番表検索」サイドバーから検索できる。

> 2026-07-30の上長定例MTGで、当初実装していた「採番表登録→自動で外部スケジュール表に行を追加する」設計（プッシュ型）が「スケジュールは上長が手動で組みたい」という意図と食い違うことが判明し、プル型に設計変更した。あわせて、完全一致による製番の自動使い回しも廃止（誤って別製品を統合するリスクを避けるため）。既存番号を使い回したい場合は、上長が試作番号欄に直接入力する運用にする。採番表は2026-07-23時点で列構成・完了日の扱い・年度またぎの連番リセット・編集権限の分担まで設計確定済み。編集権限はコード側ではなくGoogleスプレッドシートの共有設定側で管理する方針（2026-07-23決定）。

### 自動実行
- 毎週木曜 8:00: 週次集計（①②）＋完了製品アーカイブ＋未入力リマインド（メール登録者のみ）
- 毎月1日 8:00: 月次集計（③〜⑧）＋サマリー2枚の生成＋月別アーカイブ

### Apps Scriptプロジェクトの構成（2026-07-28判明・2026-08-06に3本目追加）
`Code.gs`と`WeeklyReport.gs`は**別々のApps Scriptプロジェクト**。同じプロジェクトに同居していると誤認していたことが2026-07-28に判明した。2026-08-06にさらに3本目のプロジェクトを追加。
- `Code.gs`＋`index.html`（Webアプリ本体）→ **「試作日報：ログ取り」**スプレッドシートに紐づくプロジェクト
- `WeeklyReport.gs`（集計・製番同期・トリガー類）→ **「WeeklyReport」**スプレッドシート（`REPORT_CONFIG.reportSSId`）に紐づくプロジェクト
- `SeibanSearch.gs`＋`SearchSidebar.html`（採番表検索サイドバー）→ **外部スケジュール表**（`REPORT_CONFIG.scheduleSSId`）に紐づくコンテナバインドスクリプト（読み取り専用、書き込みなし）

このため、`onOpen()`のようなスプレッドシート単位のカスタムメニュー・シンプルトリガーは、コードが実際に紐づいているスプレッドシート側でしか動作しない。今後これら3プロジェクトのいずれかに新しいトリガーやメニューを追加する際は、**Apps Scriptエディタのタイトルバーに表示されるスプレッドシート名で紐付け先を確認してから**着手すること（GASエディタのタイトルが「試作日報：ログ取り」「WeeklyReport」「【スケジュール】2024.01~」等のどれかで判別できる）。

---

## 完了した機能

### 日報アプリ（Code.gs + Index.html）
- 出退勤・休憩・作業種別・製品名・フェーズ大分類/中分類・作業時間をタイムラインUIで入力・提出
- 提出時に労務費（42円/分）を自動計算、企画名・製品/販促区分を製品マスタから自動紐付け
- 設定タブで製品・職人・フェーズ大分類/中分類・その他種別のマスタ管理（追加/修正/削除/並び替え）
- 中分類マスタに「分類」列（**製作/付帯業務**）。設定タブから変更可能（2026-07-15 定例MTG決定）
- **日報入力UIまとめ改修（2026-08-06実装・動作確認済み・コミット`e14befd`）**: 2026-07-27に決定した3点セットをまとめて実装
  - SOP運用の統一: 「サンプル製品名称＋フェーズ大分類=SOP」に一本化。「その他」種別にSOP項目は追加せず、既存の「〇〇 SOP」という製品名の自動統合・変更も行わない
  - 中分類の必須化: フェーズ大分類の「中分類あり」がtrueの場合のみ、クライアント（Index.html）・サーバー（Code.gs `submitReport()`）の両方で必須チェック。大分類ごとの選択肢絞り込みは見送り（現状の13項目フラット表示を維持、将来の改善候補）
  - サンプル製品名称の検索コンボボックス: `<select>`から検索可能なコンボボックスへ置き換え（外部ライブラリ不使用）。名称・製番・シーズン・ブランド・企画名のOR検索、一致度順（完全一致→前方一致→部分一致→製番→企画名→ブランド→シーズン）で表示。自由入力不可、PC/スマホ共通の2行表示（1行目=名称＋製番、2行目=シーズン/ブランド/企画名）。表示文字列と保存値は分離しており、日報ログへ保存されるのは従来通りサンプル製品名称のみ
  - 作業時間の直接入力欄: モーダルに数値入力欄を追加（最低5分、整数のみ）。既存のタイムラインバーのドラッグ調整とは併存（PC向け微調整手段として残存。スマホのドラッグタッチ対応は今回対象外）
  - `getSchedules()`に製番フィールドを追加（検索候補への表示用。日報ログの保存形式・集計キーは無変更）
  - 詳細な設計判断・スコープ外項目は[[project_report_input_ui_redesign]]参照
- **設定タブ「製品管理」の読み取り専用化（2026-08-07実装・動作確認済み・コミット`7af8487`）**: 製品は採番表・外部スケジュール表から自動同期される運用が確立したため、アプリ側の手動追加/修正/削除ボタンを削除し「👜 製品一覧（読み取り専用）」に変更。製品情報バッジに製番（未登録時は「製番未登録」と表示）を追加し、同名候補（製番違い）を選んだ後も選択内容を確認できるようにした

### 集計レポート（WeeklyReport.gs）
- ①〜⑧の集計シート（上表参照）。製品/販促内訳（⑤⑥）、区分列（⑦⑧）、フェーズ内訳12段階（⑦⑧: モック〜色増し＋試験体/修理/SOP/治具）
- ⓪サマリー2枚（製品/販促別）: KPIカード → 製作/付帯業務の内訳 → 間接業務の内訳（日報サマリーのみ）→ 案件TOP10 → 工程別配分 → 担当者別稼働 → 案件一覧 → トピック手入力欄
  - 表現ルール: 中立表現（「超過」「異常」と断定しない）・青の濃淡・円グラフ不使用
  - 工数の内訳は 製作/付帯業務/中分類未選択/中分類対象外 の4分類（対象外＝試験体・量産・エイジングサンプル・修理・SOP・治具）
- 月別自動アーカイブ: サマリー生成のたびに「2026年06月 製品」等のタブとして保存。同月再実行は置き換え（増殖しない）
- 日報未入力リマインド: 営業日判定（土日＋日本の祝日カレンダー＋「休日」シート）
- 製番（製造番号）システム: 外部スケジュール表と内部製品マスタを名前ではなく製番で紐付け、表記ゆれによる未突合を解消。10分おきの自動同期トリガー稼働中
  - 2026-07-21: 列マッピングのバグ2件（列ズレ・データ開始行ズレ）を修正、汚染データ（誤グルーピング・SB始まりの誤追加行71件）の後始末完了
  - 2026-07-24: さらに3件のバグを発見・修正・検証済み（コミット e423d77, 75e9db3）
    1. `syncSeibanToAppProductMaster_`の読み込み幅バグ（製番列1列分しか読めておらず、新規追加・バックフィルが無言で全件スキップ→317件新規追加・14件バックフィルで復旧）
    2. archiveCompletedSchedules（完了→削除）とsyncSeibanToAppProductMaster_（製番→復活）が判定基準不一致で無限に削除・復活を繰り返すバグ（sync側にも完了/中断判定を追加して解消）
    3. 「サンプル製品名称」は改名されることがある（企画名は不変）ため、製番リンク済み行の名称が追随しない・改名前の名前が孤立行として残り続けるバグ（リンク済み行も毎回最新名称に上書き更新する処理を追加。孤立していた旧名称17件は`cleanupOrphanedProducts()`で個別確認の上削除済み）
- 週次レポートの遅延入力対応（2026-07-28実装・動作確認済み）: イワブチさんの遅延まとめ入力で①②から実働が抜け落ちる問題への対応として、月次と同じ「手動refresh」方式を採用。`refreshWeeklyReport(startDate, endDate)`で指定週の①②行を削除→再生成、`refreshLastWeek()`は直近の自動集計週（先週分）を引数なしで再集計するラッパー
- 「WeeklyReport」スプレッドシートのカスタムメニュー「📋 レポート操作」（2026-07-28実装・動作確認済み）: `onOpen()`で追加。先週分の週次再集計／今月のレポート再集計／サマリー更新／製番の手動同期／未突合製品調査／未入力リマインドのテスト、をGASエディタを開かずメニューから実行できる。「今月」「先週」を自動判定する`refreshCurrentMonth()`/`runSummaryCurrentMonth()`も新設し、月が変わるたびに`run2026XX()`のような専用関数を作らなくてもメニューが使えるようにした
- **製番システム 全課題完了・プロジェクトクローズ（2026-08-06）**: 2026-07-16定例で発案した①〜⑥すべて決着
  1. 採番表新設・自動発番（2026-08-03実装・動作確認・コミット`d551b1e`）
  2. 外部スケジュール表のプル型反映（上長がA列に試作番号を入力/選択→B〜F列自動補完、インストール型トリガー`handleExternalScheduleEdit_`。同上）
  3. 番号の自動使い回しは廃止・類似候補列にヒント表示のみ（同上）
  4. 日報側での製番記録（2026-08-05実装・動作確認・コミット`bc83bc6`）: 職人は今まで通り製品名で選び、裏側で製番を自動ルックアップして日報ログB列に記録。実装時にWebアプリ再デプロイ漏れで列ズレの実害が発生したが後始末・修復済み（詳細は[[feedback_gas_deployment_version]]）
  5. 完了済み番号の検索サイドバー（2026-08-06実装・動作確認・コミット`3a8c737`）: 外部スケジュール表に「🔍 採番表検索」サイドバーを追加。試作番号・ブランド・企画名・製品名で部分一致検索、ステータスバッジ表示、コピー機能
  6. 外部スケジュール表「サンプルフェーズ」列（フェーズ／作業内容／用途が混在）の整理は、実利がないため**「対応不要」で決定・コード変更なし**（2026-08-06）
- **プロジェクトクローズ後の運用フォローアップ（2026-08-07実装・動作確認済み・コミット`7af8487`/`21f58fc`）**
  1. 孤立製品データの後始末: 名前ベース（`debugProductCountMismatch()`）・製番ベース（`debugSeibanNotInSchedule()`）両方の照合で内部製品マスタの孤立行を検出し、日報ログの使用実績を確認（`debugCheckProductUsageInLogs()`/`debugCheckSeibanOrphanUsage()`）した上で計14件削除
  2. 完了→進行中の即時反映: `handleExternalScheduleEdit_`にステータス列（O列）編集の検知を追加。完了→作業中に戻した際、10分おきの自動同期を待たず約30秒〜1分で日報アプリの選択肢に復活するように改善（作業中→完了の削除方向は従来通り毎週木曜8:00の週次のまま、意図的に非対称）
  3. `WeeklyReport.gs`の関数棚卸し: 初期セットアップ済み・バグ後始末済み・特定月ワンオフの計32関数を削除（3298行→2374行、約100個→75個）。日常運用は必ずスプレッドシート側メニュー「📋 レポート操作」を使う方針を徹底
  4. guide.html刷新: 6月から放置されていた「製品管理」手入力手順等を上記の実装変更に合わせて更新し、「Part C: 採番表・外部スケジュール表を管理する人向け」を新設（製番フォーマット・反映タイミングの説明）

---

## 未実装・保留

製番システム（採番表登録フロー・検索機能含む）は2026-08-06に全課題完了・クローズ。日報入力UIまとめ改修（SOP選択導線／中分類運用／入力負荷軽減）も2026-08-06に実装・動作確認完了。

| 機能 | 優先度 | 理由・備考 |
|------|-------|-----------|
| 職人メールアドレス登録 | 中 | 未入力リマインドの送信先。2026-07-27時点、登録は見送り・保留を継続（積極対応はしない） |
| 大分類ごとの中分類候補の絞り込み | 低 | 今回（2026-08-06）は現状の13項目フラット表示を維持。現場から要望が出た場合の改善候補 |
| guide.htmlのガイド1本化 | 低 | 2026-07-17に「UI変更が固まってから最後にやる」と決定した保留タスク。日報入力UIまとめ改修が完了したため着手候補になりうる |
| 検索一致箇所のハイライト表示 | 低 | 製品名検索コンボボックスの追加検討事項として2026-08-06に言及のみ。未着手 |
| 原価表への出力機能 | 低 | 企画別フェーズ別工数を生産管理課が転記しやすい形で出力（2026-07-17優先度見直し：中→低） |
| フェーズ別平均比較・暫定標準工数 | 低 | 半年データが溜まってから分類別中央値で作る構想 |
| 分給レートの設定値化 | 低 | 現在 `Code.gs` に `RATE_PER_MINUTE = 42` でハードコード。2026-07-17時点：42円のまま運用継続、変更なし |
| 売上データとの対比（採算分析） | 対象外 | 上長判断により生産管理課の業務。半年後にデータが揃ってから改めて判断（2026-07-17時点でも対象外を維持） |

---

## 重要な設計判断

### 1. スプレッドシート3ファイル体制（2026-07-16）
生データ／集計データ／共有用サマリーを分離。サマリーSSは閲覧専用として上長に共有でき、アーカイブが増えても集計側が重くならない。

### 2. SS参照はID直指定
`REPORT_CONFIG` の reportSSId / summarySSId でID優先。名前検索は予備（MIMEタイプでスプレッドシートのみ対象）。ファイル改名で迷子SSが量産された事故（2026-07-14）の再発防止。

### 3. 労務費は42円/分（間接費込み）
原価表（生産管理課管轄）の直接分給36.8円/分とは別の値。用途が異なるので混同しない。

### 4. 追記型シートの重複防止と列変更ガード
①〜⑧は既存の週/月をスキップ（何度実行しても重複しない）。⑦⑧は列構成変更時に既存データがあると追記を中止してログ警告（列ずれ防止）→ タブ削除して `run2026XX()` で再生成する。

### 5. 中分類の分類（製作/付帯業務）はマスタ駆動
「フェーズ中分類」シートD列で管理し、設定タブから変更可能。名称や項目が上長確認で変わっても再コーディング不要。集計側は `DEFAULT_SUBCAT_CLASS` で未設定を補完（旧名称「型紙・抜き型作成/修正」にも対応）。

### 6. サマリーの表現ルール（2026-07-14）
標準工数が未設定のため評価的表現を避ける。色は青の濃淡（濃青=上位20%、薄青=20〜40%）。赤は未入力・データ不整合など客観的異常のみ。担当者別は「稼働状況」表記で効率比較にしない。

### 7. 製品名は改名されうる・企画名は不変（2026-07-24）
外部スケジュール表の「サンプル製品名称」は試作の途中で改名されることがあるが、「企画名」は変わらない（嶋谷さんへの確認で判明）。製番（発行時にブランド・企画名・製品名から1回だけ計算されるキー）自体は改名の影響を受けないが、名前ベースの突合ロジック（`archiveCompletedSchedules`・`syncSeibanToAppProductMaster_`）は改名に対して脆弱だったため、リンク済み行の名称を毎回最新に上書きする処理を追加済み。日報ログ自体には製番が記録されておらず（`L`定数に製番インデックスなし）、過去ログの遡及的な名寄せは現状不可。今後名前ベースの突合が必要な場面では企画名を優先する方が安全。

### 8. 完了/中断判定ロジックの重複（要将来対応）
`archiveCompletedSchedules()`（完了→内部製品マスタから削除）と`syncSeibanToAppProductMaster_()`（製番があれば復活/追加）が、どちらも独自に「全行が完了/中断か」を判定しており（`ARCHIVE_STATUSES`が2箇所に重複定義）、2026-07-24に判定基準の不一致で無限削除・復活バグを起こした。修正はしたが重複自体は解消していないため、将来この2関数のどちらかを触る際は両方への影響を必ず確認する（共通関数への切り出しは未実施）。

### 9. 製番の遡及統合はしない方針（2026-08-07・カブセトート）
旧製番→新代表製番への統合（例: TK-G27-0103/0101/0098 → TK-G27-0006「カブセトート代表」）が採番表側で発生した場合、**過去の日報ログには一切触れず、今後の新規登録から新製番に統一する**運用に確定（コード側での自動統合ロジックは意図的に作らない）。このため⑤⑥⑦などの集計レポートでは、統合対象の製品の工数が新旧製番で複数行に分裂したまま残り続ける。ブランド別・企画別・製品別レポートでこの種の製品の数字を見る際は、関連する旧製番・新製番すべてを人間側で横断的に合算する必要がある。

---

## 手動操作チートシート（GASエディタ）

2026-07-28以降は、下記のうち週次・月次・サマリー更新／製番同期／各種調査は「WeeklyReport」スプレッドシートのメニュー「📋 レポート操作」からGASエディタを開かずに実行できる（`onOpen()`で追加。`WeeklyReport.gs`のプロジェクトは「WeeklyReport」スプレッドシートに紐づいている。「試作日報：ログ取り」に紐づくのは`Code.gs`側で別プロジェクト）。

| やりたいこと | 関数 | メニューから実行可 |
|-------------|------|:---:|
| 今週分（先週分）を今のデータで再集計 | `refreshLastWeek()`（特定週なら`refreshWeeklyReport(startDate, endDate)`） | ✅ |
| 今月のレポートを再集計（③〜⑧＋サマリー） | `refreshCurrentMonth()`（特定月なら`refreshMonthlyReport(year, month)`） | ✅ |
| サマリーだけ更新（何度でも安全） | `runSummaryCurrentMonth()`（特定月なら`generateSummarySheet(year, month)`） | ✅ |
| 製番の手動同期（外部スケジュール表→内部製品マスタ） | `runSeibanSync()` | ✅ |
| 未突合製品の調査 | `debugProductCountMismatch()` | ✅ |
| 未入力リマインドのテスト（送信なし） | `debugMissingReports()` | ✅ |
| 特定月の月次を再集計（③〜⑧＋サマリー・過去月固定） | `run202605()` / `run202606()` / `run202607()` ※7月分の先取りは月末に該当行削除→再実行が必要 | — |
| 週次バックフィル | `generateWeeklyReportBackfill()`（START_DATE_STR を変更して実行） | — |
| 名前ベースの未突合調査（旧） | `debugUnmatched()` | — |
| 使い方ガイドDoc生成 | `createUsageGuideDoc()`（実行するたび新規Docができる） | — |

⚠ `generateMonthlyReportForMonth` をドロップダウンから直接実行しない（引数なしで不正データが書き込まれる）。必ずラッパー経由で。

採番表の検索は、外部スケジュール表側の「🔍 採番表検索」（図形ボタン、またはカスタムメニュー）から使う。GASエディタを開く必要はない（`SeibanSearch.gs`は「WeeklyReport」ではなく外部スケジュール表に紐づく別プロジェクト）。
