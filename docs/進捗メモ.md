# 進捗メモ

## 現在の状態（2026-06-09時点）

- `generateWeeklyReport` 実行済み → ①②に 0602-0608 の1行追記済み
- `generateWeeklyReportBackfill`（START: 2026/05/26）は**未実行** → 要実行
- `generateMonthlyReportForMonth(2026, 5)` の5月分は**未実行** → 要実行
- トリガー未設定（`setupWeeklyTrigger` / `setupMonthlyTrigger` 未実行）

---

## 次にやること

1. GASエディタで `generateWeeklyReportBackfill` を実行（0526-0601の週次を①②に追記）
2. GASエディタで `generateMonthlyReportForMonth(2026, 5)` を実行（5月の月次を生成）
3. `setupWeeklyTrigger()` を1回実行（毎週木曜 7:00 自動実行を設定）
4. `setupMonthlyTrigger()` を1回実行（毎月1日 7:00 自動実行を設定）

---

## 関数一覧（WeeklyReport.gs）

### 通常実行（自動トリガー）

| 関数名 | タイミング | 内容 |
|--------|-----------|------|
| `generateWeeklyReport()` | 毎週木曜 7:00 | 週次レポート更新（①②③シート） |
| `generateMonthlyReport()` | 毎月1日 7:00 | 月次レポート更新（④⑤⑥シート・先月分） |

### 手動実行

| 関数名 | 内容 |
|--------|------|
| `generateMonthlyReportForMonth(year, month)` | 特定月の月次レポート生成（例: `(2026, 5)`） |
| `generateWeeklyReportBackfill()` | START_DATE_STR から今日まで週次を一括生成 |

### トリガー設定（初回1回のみ）

| 関数名 | 内容 |
|--------|------|
| `setupWeeklyTrigger()` | 毎週木曜 7:00 トリガー設定 |
| `setupMonthlyTrigger()` | 毎月1日 7:00 トリガー設定 |

### デバッグ

| 関数名 | 内容 |
|--------|------|
| `testAccess()` | 日報ログSS・スケジュールSSへのアクセス確認 |
| `debugWeeklyReport()` | 集計期間内のログ一覧・突合チェック |
| `debugUnmatched()` | 未突合製品の類似候補を出力 |
