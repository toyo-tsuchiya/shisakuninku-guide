# PROJECT_STATUS.md
最終更新: 2026-06-09

---

## プロジェクト目的

試作課の工数・労務費を日報から自動集計し、上長・生産管理課が参照できる形にする。

- **短期目的（現フェーズ）**: まず記録・蓄積。「何に何時間使ったか」を定量化する
- **中期目的**: ブランド・企画別のリソース配分を可視化し、上長の事業判断をサポート
- **長期目的**: フェーズ別工数トレンドの分析（③なぜ時間がかかったか）、改善施策立案

> 上長の言葉: 「感覚で試作課を運営する段階を卒業して、数字で試作課を経営したい」

---

## 完了した機能

### 日報アプリ（Code.gs + Index.html）
- 職人が日報を入力・提出するWebアプリ（GAS）
- 出退勤・休憩・作業種別・製品名・フェーズ・作業時間を記録
- 提出時に **労務費（42円/分）** を自動計算してログに書き込む
- 製品名からスケジュールSSの企画名を自動紐付け（Q列: 企画名）

### 集計レポート（WeeklyReport.gs）

| # | シート | 更新方式 | 内容 |
|---|-------|---------|------|
| ① | 週次推移 | 毎週追記 | 全体の実働・製造比率・労務費 |
| ② | 職人別週次 | 毎週追記 | 職人ごとの週次実績 |
| ③ | 案件進捗 | 毎週上書き | 全案件の累計工数・残日数・担当者 |
| ④ | 月次推移 | 毎月追記 | 全体の月次KPI |
| ⑤ | ブランド別 | 毎月追記 | ブランドごとの月内+累計工数 |
| ⑥ | 企画別 | 毎月追記 | 企画ごとの詳細（フェーズ別累計含む） |
| ⑦ | 職人別 | 毎月追記 | 職人×ブランド×企画の月内+累計工数 |

### サポート機能
- `generateWeeklyReportBackfill()` — 過去日付から週次を一括生成
- `generateMonthlyReportForMonth(year, month)` — 特定月の月次を手動生成
- `setupWeeklyTrigger()` / `setupMonthlyTrigger()` — 自動実行トリガー設定
- `debugUnmatched()` — 未突合製品の類似候補をログ出力
- `debugWeeklyReport()` — 集計期間内のログと突合状況を確認
- `testAccess()` — 外部SSへのアクセス確認

### ドキュメント
- `docs/要件定義.md` — システム目的・フロー・費用計算
- `docs/DB設計.md` — スプレッドシート構造・突合ロジック
- `docs/進捗メモ.md` — 実行状況・関数一覧・次やること
- `docs/セットアップ手順.md` — GAS初期設定・デプロイ手順
- `docs/使い方ガイド.md` — 各シートの役割・活用法・入力ミスの調べ方

---

## 現在作業中

- なし（本日の開発セッションで主要機能が一通り完成）

---

## 未実装機能

| 機能 | 優先度 | 理由・備考 |
|------|-------|-----------|
| 売上データとの対比（採算分析） | 低 | 上長判断により生産管理課の業務。試作課のスコープ外 |
| フェーズ別平均比較（学習曲線の可視化） | 低 | データが3ヶ月以上蓄積されてから意味をなす |
| 原価表への出力機能 | 中 | 企画別フェーズ別工数を生産管理課が転記しやすい形で出力 |
| 分給レートの設定値化 | 低 | 現在 `Code.gs:13` に `RATE_PER_MINUTE = 42` でハードコード |

---

## 次回やること

1. **⑥企画別の5月データを再生成する**
   - 集計レポートSSの⑥企画別シートにある5月行を削除
   - GASエディタで `generateMonthlyReport` を実行（フェーズ別累計列が追加される）

2. **トリガーを設定する**
   - GASエディタで `setupWeeklyTrigger()` を実行（毎週木曜 7:00 自動実行）
   - GASエディタで `setupMonthlyTrigger()` を実行（毎月1日 7:00 自動実行）

3. **上長への確認・承認**
   - 現在の集計シート構成（①〜⑦）を上長に確認してもらう
   - 「今後の活用」の優先順位をすり合わせる

4. **GitHubにpushする**
   - 本日の変更（⑥フェーズ追加・⑦職人別追加・ドキュメント整備）をコミット・push

---

## 重要な設計判断

### 1. シートを固定7枚にした
旧システムは週ごとにシートが増える設計で、数十枚のタブが残骸として残っていた。現在は固定7シートに週次・月次を追記していく方式。SSが肥大化しない。

### 2. 労務費は42円/分（間接費込み）
原価表（生産管理課管轄）の直接分給36.8円/分とは別の値。試作課内の管理用コストとして間接費15%を上乗せした概算値。レート変更時は `Code.gs:13` の `RATE_PER_MINUTE` を修正するだけでよい設計。

### 3. 月内に動きがある案件のみ追記（⑤⑥⑦）
全案件の「現在状態」は③案件進捗（毎週上書き）に任せる。④〜⑦の月次シートは「その月に動いた案件だけ」を追記するため、無関係な案件でシートが埋まらない。

### 4. 突合ロジックの優先順位
日報の製品名・企画名とスケジュールSSを突合する際の優先順位：
1. 日報の企画名 → スケジュールSSの企画名
2. 日報の製品名 → スケジュールSSの製品名
3. 日報の製品名 → スケジュールSSの企画名経由で逆引き

突合できない場合は「未紐付け」として集計に含める（除外しない）。

### 5. 利益分析はスコープ外
上長から「②利益は出ているか」の分析は生産管理課の業務と確認済み。試作課は工数データを正確に提供する役割。売上データを持ち込む仕組みは現時点では作らない。

### 6. フェーズキーの固定
⑥企画別のフェーズ別累計列は `[モック, 1st, 2nd, 3rd, その他]` の5列に固定。
スプレッドシートでの集計・グラフ化のしやすさを優先した。それ以外のフェーズ名は「その他」に集約される。
