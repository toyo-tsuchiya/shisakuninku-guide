# HANDOVER.md
作成日: 2026-06-09　最終更新: 2026-06-17（3回目）

> ⚠️ このファイルは2026-06-17時点で更新が止まっています。最新の状況は [PROJECT_STATUS.md](PROJECT_STATUS.md) を参照してください。

新しいチャットセッションへの引き継ぎドキュメントです。

---

## プロジェクトの目的

試作課の工数・労務費を日報から自動集計し、上長・生産管理課が参照できる形にする。

- 職人が毎日の作業内容を日報アプリ（GASウェブアプリ）から入力
- GASが自動集計し、週次・月次レポートをGoogleスプレッドシートに生成
- 上長が「どの事業に何時間使ったか」を数字で把握できるようにする

> 上長の言葉: 「感覚で試作課を運営する段階を卒業して、数字で試作課を経営したい」
> 現フェーズは「まず記録・蓄積」。分析は3ヶ月以上データが溜まってから。

---

## 現在完成している機能

### 日報アプリ（Code.gs + Index.html）
- 職人が出退勤・休憩・作業種別・製品名・フェーズ・作業時間を入力・提出するWebアプリ
- 提出時に労務費（42円/分）を自動計算してログに記録
- 製品名からスケジュールSSの企画名を自動紐付け（日報ログのQ列: 企画名）

### 集計レポート（WeeklyReport.gs）
全7シートの固定構成。シートは増えない設計。

| # | シート名 | タブ色 | 更新方式 | 内容 |
|---|---------|--------|---------|------|
| ① | 週次推移 | 青 | 毎週追記 | 全体の実働・製造比率・労務費・フルニンク・実績ニンク |
| ② | 職人別週次 | 水色 | 毎週追記 | 職人ごとの週次実績 |
| ③ | 案件進捗 | 緑 | 毎週上書き | 全案件の累計工数・残日数・担当者 |
| ④ | 月次推移 | 黄 | 毎月追記 | 全体の月次KPI |
| ⑤ | ブランド別 | 薄黄 | 毎月追記 | ブランドごとの月内+累計工数 |
| ⑥ | 企画別 | 紫 | 毎月追記 | 企画ごとの詳細＋フェーズ別累計（モック/1st/2nd/3rd） |
| ⑦ | 職人別 | 橙 | 毎月追記 | 職人×ブランド×企画の月内+累計工数 |
| ⑧ | 職人別月次 | 青緑 | 毎月追記 | 職人ごとの月次実績（②の月次版） |

### サポート関数
| 関数名 | 用途 |
|-------|------|
| `generateWeeklyReport()` | 週次レポート（毎週木曜自動実行予定） |
| `generateMonthlyReport()` | 月次レポート（毎月1日自動実行予定）前月を自動判定 |
| `generateMonthlyReportForMonth(year, month)` | 特定月を手動生成（ラッパー関数経由で実行） |
| `generateWeeklyReportBackfill()` | 過去日付から週次を一括生成 |
| `setupWeeklyTrigger()` | 毎週木曜7時の自動トリガー設定（初回1回のみ） |
| `setupMonthlyTrigger()` | 毎月1日7時の自動トリガー設定（初回1回のみ） |
| `debugUnmatched()` | 未突合製品の類似候補をログ出力 |
| `debugWeeklyReport()` | 集計期間内のログと突合状況を確認 |
| `testAccess()` | 外部SSへのアクセス確認 |
| `countBusinessDays(start, end)` | 期間内の営業日数を計算（土日除く）。フルニンク計算に使用 |

### ドキュメント（docs/）
| ファイル | 内容 |
|---------|------|
| `docs/要件定義.md` | システム目的・製品試作フロー・費用計算の考え方 |
| `docs/DB設計.md` | スプレッドシート構造・突合ロジック |
| `docs/進捗メモ.md` | 実行状況・関数一覧・次やること |
| `docs/セットアップ手順.md` | GAS初期設定・デプロイ手順 |
| `docs/使い方ガイド.md` | 各シートの役割・活用法・入力ミスの調べ方 |

---

## 現在の課題・未完成の機能

| 機能 | 優先度 | 状況 |
|------|-------|------|
| **トリガー設定** | 🔴 次回 | `setupWeeklyTrigger` / `setupMonthlyTrigger` を実行（上長確認後） |
| **`createUsageGuideDoc()` の実行** | 🔴 次回 | 関数の更新は完了。GASエディタから1回実行してGoogleドキュメントを生成し、URLを上長に共有する |
| **①週次推移の過去データ更新** | 🟡 中 | フルニンク/実績ニンク列が追加されたため、①シートを削除して週次を再生成すると過去データも最新列構成になる（削除は任意）|
| 日報の作業種別細分化 | 🟡 中 | 上長相談中。「型紙作成」「型紙修正」「原価表」などを追加予定。「その他」を分解したい |
| ⑦職人別シートの見直し | 🟡 中 | 上長相談中。職人×ブランド粒度は不要かも。シンプル化を検討 |
| 原価表への出力機能 | 🟡 中 | 企画別フェーズ別工数を生産管理課が転記しやすい形で出力 |
| Googleカレンダー連携 | 🟢 低 | 本部長発案。日報実績を試作課共有カレンダーに自動記録。設計メモ済み |
| 分給レートの設定値化 | 🟢 低 | 現在 `Code.gs:13` で `RATE_PER_MINUTE = 42` にハードコード |
| フェーズ別平均比較 | 🟢 低 | データ3ヶ月以上蓄積後に意味をなす。今は不要 |
| 利益分析（売上対比） | ❌ 対象外 | 上長の判断により生産管理課の業務。試作課のスコープ外 |

---

## 使用技術

| 技術 | 用途 |
|-----|------|
| Google Apps Script (GAS) | バックエンド・集計処理・ウェブアプリ本体 |
| JavaScript (ES6+) | GASコード全般 |
| HTML / CSS | 日報入力UI（Index.html、guide.html） |
| Google Sheets | データ保存・レポート出力 |
| Google Drive API | 集計レポートSSの検索・作成 |
| Git / GitHub | バージョン管理（orient-11/shisaku-ninku-app） |

---

## ファイル構成

```
sample-section/
├── README.md               # 概要・ファイル構成・ドキュメントへのリンク
├── PROJECT_STATUS.md       # 完了機能・設計判断・次回やること
├── HANDOVER.md             # このファイル
├── Code.gs                 # 日報アプリのバックエンド（提出・マスタ管理）
├── WeeklyReport.gs         # 週次・月次集計レポート生成
├── Index.html              # 日報入力UI
├── guide.html              # 利用ガイドページ
├── sample_preview.html     # UIプレビュー用
├── 参考原価表.png
├── 最新：参考原価表トート小原価表.csv
└── docs/
    ├── 要件定義.md
    ├── DB設計.md
    ├── 進捗メモ.md
    ├── セットアップ手順.md
    └── 使い方ガイド.md
```

### 外部スプレッドシート（コード内で参照）

| 用途 | ID / 名前 |
|------|---------|
| 日報ログSS | `15FXg3_YFyA-JGVXhtINm7H5jtlO6l07l0wPDezGgqGs` |
| スケジュールSS | `1vdejHfw6fbgVNJc0BuV3xYEWpNNc6lE5iaQYIIJ-QFs` |
| 集計レポートSS | 名前「試作課週次レポート」で検索・自動作成 |

---

## 2026-06-09（2回目）完了した内容

- `docs/使い方ガイド.md` が前回セッションで既に作成済みであることを確認
- `createUsageGuideDoc()` を `WeeklyReport.gs` に追加（GASから1回実行でGoogleドキュメント生成）
- GitHub push 完了（コミット: `54dd5d4`）

## 2026-06-17（3回目）完了した内容

- **日報アプリ（Code.gs + Index.html）を大幅改修**

### 新機能①：フェーズを「大分類」「中分類」に分割
- フェーズ大分類（旧ステージ）：14件に更新（モック/1st/2nd/3rd/4th/5th/最終/色増しサンプル/試験体/量産/エイジングサンプル/修理/SOP/治具）
- フェーズ中分類：新規マスター「フェーズ中分類」シートを追加（初期値5件）
- 大分類に「中分類あり」フラグを設定可能。フラグONの大分類を選ぶと中分類ドロップダウンが出現（任意選択）
- 日報ログにR列「フェーズ中分類」を追加（既存データは空、後方互換あり）

### 新機能②：設定タブに修正ボタン追加
- 製品管理・フェーズ大分類管理・フェーズ中分類管理のすべてに「修正」ボタンを追加
- クリックするとフォームに値が入り「更新」「キャンセル」ボタンに切り替わる

### その他
- `setup()` を再実行するだけで既存シートへのマイグレーション（R列追加・D列追加）が自動実行される
- チェックボックスのCSSバグ（グローバルinputスタイルが適用される問題）を修正

---

## 2026-06-17（2回目）完了した内容

- **②職人別週次：タブ削除 → `generateWeeklyReportBackfill` で再生成完了**
- **④月次推移：GASコードが古かったため再貼り付け → タブ削除 → `run202605` で再生成**
  - `実稼働人工` 列が正しい値（93）で出力されたことを確認
- **シート順番を変更**（reorderSheets 関数を追加）
  - ①週次推移 / ②職人別週次 / ③月別推移 / ④職人別月次 / ⑤ブランド別 / ⑥企画別 / ⑦製品別進捗
- **③案件進捗 → ⑦製品別進捗に全面改修**
  - 列構成：製品名・企画名・ステータス・フェーズ・累計工数・累計労務費・納品希望日・残日数
  - 完了・中断は行レベルで除外（同一製品名の完了済み行と予定行を別扱い）
  - スケジュールSSの読み込み開始行を6行目に修正
  - S.status = 12（M列）に修正、scheduleRows の取得列数を13に変更
  - `runProjectProgress` ラッパー追加（単体実行用）
- **⑦製品別進捗については上長相談後に要否を判断**（内容の必要性を確認中）

## 2026-06-17（1回目）完了した内容

- **①週次推移：タブ削除 → `generateWeeklyReportBackfill` で再生成**
  - 実稼働人工・フルニンク列が正常に生成されたことを確認
- **⑧職人別月次：`run202605` で5月分を追記済み**
- **undefined不正行のクリーンアップ完了**（`cleanupUndefinedRows` 実行）
- **GAS上での動作確認完了**

---

## 2026-06-16 完了した内容

- **①週次推移の「稼働人数」→「実稼働人工」に変更**（`appendToWeeklyTrend` 改修）
  - 列3: 稼働人数（ユニーク人数）→ 実稼働人工（ユニーク職人×日数）
  - 列6: 実績ニンク(人工)（重複）→ 稼働人数(参考)（ユニーク人数）
  - ヘッダー自動更新ロジックを列名比較方式に変更（列数変化なくても更新される）
- **④月次推移の「稼働人数」→「実稼働人工」に変更**（`appendToMonthlyTrend` 改修）
  - actualByKey.size（ユニーク職人×日数）を新たに計算して採用
  - ヘッダー自動更新ロジックを追加
- **⑦職人別を月次レポートから除外**（`generateMonthlyReportForMonth` から呼び出し削除）
  - 既存の⑦シートはスプレッドシート上に残る（手動削除が必要）
- **`overwriteProjectProgress` のバグ修正**（固定行全削除エラー）
  - `deleteRows` → `clearContent` + `deleteRows(3以降)` に変更
- **`cleanupUndefinedRows` / `run202605` ラッパー関数を追加**（不正データ修復用）
- **`generateWeeklyReportBackfill` の START_DATE_STR を '2026/05/01' に変更**
- GAS への貼り付けは完了。`run202605` と `generateWeeklyReportBackfill` の実行は次回確認待ち

## 2026-06-15 完了した内容

- **①週次推移に人工（ニンク）2列を追加**（`appendToWeeklyTrend` 改修）
  - `フルニンク(人工)`：集計期間の営業日数（土日除く）× 稼働人数
  - `実績ニンク(人工)`：実際に日報を提出した職人×日のユニーク数
  - `countBusinessDays()` ユーティリティ関数を新規追加
  - 既存①シートに対してヘッダーを自動更新する処理も追加
- **⑧職人別月次シートを新設**（`appendToWorkerMonthly` 新規追加）
  - ②職人別週次の月次版。職人×月で1行、タブ色：青緑（`#4DD0E1`）
  - `generateMonthlyReportForMonth` から自動呼び出しに追加済み
- **`createUsageGuideDoc()` を全面更新**
  - ①のフルニンク/実績ニンク説明を追加
  - ⑧職人別月次のセクションを追加
  - 「各シートの列定義と計算式」セクションを全シート分追加（①〜⑧）
- **`docs/使い方ガイド.md` を更新**
  - ①の活用表・列定義にフルニンク/実績ニンクを追加
  - ⑧職人別月次のシート説明・列定義を追加
  - 入力ミスの影響表を①②④⑧、②⑦⑧に更新
- GitHub push 完了

## 2026-06-12 完了した内容

- **`docs/使い方ガイド.md` に「各シートの列定義と計算式」セクション追加**
  - 共通用語の定義（実働・製造・間接・労務費の計算ロジック）
  - ①〜⑦ 全シートの全列について「何を・どう計算しているか」を一覧化
  - ブランド紐付けの仕組み・「未紐付け」の発生条件なども注記
- **上長相談内容を整理**（以下の要望を確認・次回実装予定）
  - ①週次推移に稼働ニンク追加（営業日フルニンク vs 実績ニンク）
  - 職人別月次シートの新設（②職人別週次の月次版）
  - 日報の作業種別細分化（「その他」を「型紙作成」「原価表」等に分解）← 別途相談継続
- **Googleカレンダー連携のアイデアを記録**（本部長発案・優先度低・設計メモ保存済み）
- **`createUsageGuideDoc()` の更新方針を決定**（次回セッションで実施）

## 2026-06-10 完了した内容

- **⑥企画別シートの列構成を全面改修**（コミット: `e83cffe`）
  - `フェーズ内訳（月内）`（1セルのテキスト）→ `月内_モック/1st/2nd/3rd/その他` の5列に分割
  - `ステータス` 列をA列（非表示）に移動。`labelExists()` に列番号パラメータを追加
  - 列構成: 26列（A=ステータス非表示、B=年月〜P=計画完了、Q〜U=月内フェーズ、V〜Z=累計フェーズ）
- HANDOVER.md の Step 1 手順を「シートタブごと削除 → 再生成」に修正
- GitHub push 完了（最新コミット: `e83cffe`）

---

## 次回最初にやること

### Step 1: シートの整理（次回最初に実施）

以下を手動で対応する：
- **旧タブの削除**：スプレッドシートに `③月別推移`・`④職人別月次` の旧名タブ（`④月次推移`・`⑧職人別月次`）が残っていれば手動で名前変更または削除して再生成
- **`reorderSheets`** を実行してタブ順を整える
- **⑦職人別タブ**（旧）が残っていれば右クリック → 削除

> ✅ ①週次推移・②職人別週次・③月別推移（旧④）・④職人別月次（旧⑧）・⑦製品別進捗 はコード修正済み

### Step 1.5: フェーズ大分類の「中分類あり」フラグを設定する
スプレッドシートの「ステージ」シートを開き、D列（中分類あり）に設定：
- `TRUE`（中分類を使う）: モック / 1st / 2nd / 3rd / 4th / 5th / 最終 / 色増しサンプル
- `FALSE`（中分類なし）: 試験体 / 量産 / エイジングサンプル / 修理 / SOP / 治具
> または設定タブ → フェーズ大分類管理 の「修正」ボタンからも設定可能

### Step 2: ⑦製品別進捗の要否を上長に確認
上長相談後、不要であれば `overwriteProjectProgress` 関数ごと削除してよい。

### Step 3: トリガーを設定する（上長確認後）
GASエディタで以下を順番に実行（各1回のみ）：
1. **`setupWeeklyTrigger`** → 毎週木曜7:00の自動実行が設定される
2. **`setupMonthlyTrigger`** → 毎月1日7:00の自動実行が設定される
3. GASエディタ左メニュー「トリガー」から設定済みか確認する

### Step 4: 使い方ガイドの Google ドキュメントを作成する
GASエディタで **`createUsageGuideDoc`** を実行（1回のみ）：
1. 実行ログに `✓ ガイドドキュメント作成完了: https://docs.google.com/...` が出ることを確認
2. URLをコピーして上長に共有する
> ⚠️ 2回実行すると同名ドキュメントが2つできる。1回だけ実行すること。

### Step 5: 上長への確認
①〜⑧のシート構成（⑦なし）と使い方ガイドを上長に見せて確認をもらう。

### Step 6: GitHub push
上記の作業後に変更があればpushする。

---

## 注意事項

### GASの実行について
- **`generateMonthlyReportForMonth` をドロップダウンから直接実行しない**
  引数なしで実行されると `undefined年undefined月` の不正データが書き込まれる。
  特定月を実行したい場合はラッパー関数を作成する：
  ```javascript
  function run202605() { generateMonthlyReportForMonth(2026, 5); }
  ```
- **`generateMonthlyReport`（引数なし版）は6月に実行すると自動で5月を集計する**
  `d.getMonth()` = 5（6月）→ 先月の5月を自動判定する仕組み。

### スプレッドシートについて
- 集計レポートSSには旧システムの週別シート（`①サマリー_0521-0527` など）が非表示で残っている。
  現行コードはこれらを一切参照・更新しない。混乱するなら削除してOK。
- ⑦職人別シートは新規データが追加されなくなった。タブを右クリック→「シートを削除」で手動削除してよい。

### コストについて
- 労務費の分給 **42円/分** は間接費込みの試作課内管理用の値。
- 生産管理課が使う原価表の直接分給（36.8円/分）とは用途が異なる。混同しない。
- レート変更時は `Code.gs:13` の `RATE_PER_MINUTE = 42` を変えるだけでよい。

### GitHub
- リポジトリ: `orient-11/shisaku-ninku-app`
- ブランチ: `main`
- 最新コミット: `e83cffe` feat: ⑥企画別シートのフェーズ列をセル分割・ステータス列を先頭非表示に変更
